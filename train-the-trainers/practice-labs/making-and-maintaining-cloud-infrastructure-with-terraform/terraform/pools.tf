# Example: Create a Proxmox Pool using Terraform.
# Uncomment code below, run `terraform plan`, then `terraform apply`.

#resource "proxmox_virtual_environment_pool" "test_pool" {
#  comment = "Managed by terraform."
#  pool_id = "mypool"
#}
